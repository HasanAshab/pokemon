make some armors data for my Magical Battle game. 

here are some example armor data

latherarmor1: {
  type: "armor",
  covers: 20, // this means how much percentage of the body this armor covers (technically armor trigger chance)
  stats: {
    // the stat value used to determine damage e.g pokemon games

    def: 20 // this means can handle physical damage
    spd: 1 // this armor can handle a very little special attack
  },
  tokens: {
    // how much stat will change to the user

    spe: -1 // using this, a very little speed stat of user is decreased ( as lather armors are usally not heavy) 
  }
},
locket1: {
  type: "armor",
  covers: 50,
  stats: {
    def: 0 // this means the locket can handle no physical attack (as it is magical)
    spd: 20 // this magical locket can handle special damage
  }
},

here are some notes:
1. the data should be realistic that thrills the player.
2. some armors can handle only phy atk some can only special atk and some can handle both,
 its completely dynamic but be reasistic

