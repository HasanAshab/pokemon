edit #Current File add a button after kingdoms containers, Disaster Report. when clicked show what disaster is happening at each kingdom. 
use kingdoms[name].disaster.current obj (e.g. { Earthquake: 10, Tsunami: 5 })



Notes:
1. you will get all disaster at #constraints.js 
2. only a single disaster can happen at a time but
also can be multiple if there are related disasters of the triggered disaster
3. show power of the disaster using 1 to 10. the chance of power is in that order
(from most probable to least probable)
 4 to 6
 1 to 3
 7 to 10
If a disaster is more prone to a kingdom than the probability
of happening a heigher power increases
4. you can get disaster state to a kingdom here: kingdoms[name].disaster.geoState (e.g. {Flood: "normal", Cyclone: "prone", Earthquake: "immune"})
normal: disasters has 5% chance to occure
prone: disasters will have 10% chance
immune: disasters will never happen
related normal: disasters has 15% chance to occure
related prone: disasters will have 30% chance
related immune: disasters will never happen

Psudo Algorithm:
Shuffle the disaster -> loop it -> if a disaster happened stop the loop 
-> predict a power 1 to 10 -> loop through related disasters
-> if a happended save it and keep going -> all related disasters power will be
rounded half of the parent disaster. e.g Earthquake 10 = Tsunami 5
-> at last store them as kingdoms[name].disaster.current = { Earthquake: 10, Tsunami: 5 }